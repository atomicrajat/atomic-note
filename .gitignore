# Build output
.build/
build/
*.bin
*.elf
*.map

# Secrets — never commit credentials
secrets.h
firmware/atomic_note/secrets.h

# Companion runtime state lives in $HOME, not here, but be explicit
*.jsonl
sync-state.json

# Python bytecode from the companion service
__pycache__/
*.pyc

# Reference material and scratch work, kept locally and never published
027*/
scratch/

# macOS
.DS_Store
